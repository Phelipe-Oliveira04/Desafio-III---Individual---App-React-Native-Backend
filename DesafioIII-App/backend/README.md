
# Backend - Desafio III
Setup:
1. cd backend
2. npm install
3. configure .env with MONGO_URI
4. npm start
API endpoints:
GET /reports
GET /reports/:id
POST /reports
PUT /reports/:id
DELETE /reports/:id
